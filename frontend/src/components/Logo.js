import React from 'react';
import LogoRizzder from '../../public/LogoRizzder.png'; // Adjust the path to where your LogoRizzder.png is located

const Logo = () => {
  return (
    <div>
      <img src={LogoRizzder} alt="LogoRizzder" />
    </div>
  );
};

export default Logo;
